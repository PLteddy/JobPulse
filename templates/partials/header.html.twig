{# templates/partials/header.html.twig #}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/header.css') }}">
{% endblock %}

<header class="site-header">
   
            <div class="header-container">
                <a>
                    <img src="{{ asset('img/logo.png') }}" alt="Job Pulse" class="header-logo">
                </a>
                <nav class="header-nav">
                    <a href="{{ path('app_login') }}">Connexion</a>
                    <a href="{{ path('inscription', { type: 'etudiant' }) }}">Inscription Étudiant</a>
                    <a href="{{ path('inscription', { type: 'entreprise' }) }}">Inscription Entreprise</a>
                    <a href="{{ path('inscription', { type: 'tuteur' }) }}">Inscription Tuteur</a>
                </nav>
            </div>
</header>