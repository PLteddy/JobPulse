{# templates/partials/header.html.twig #}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/header.css') }}">
{% endblock %}

<header class="site-header">
   
            <div class="header-container">
                <a>
                    <img src="{{ asset('images/logo.png') }}" alt="Job Pulse" class="header-logo">
                </a>
                <nav class="header-nav">
                    <a href="{{ path('app_logout') }}">Deconnexion</a>
                    <a href="{{ path('inscription', { type: 'etudiant' }) }}">Mes candidatures</a>
                    <a href="{{ path('inscription', { type: 'entreprise' }) }}">Mes enregistrements</a>
                    <a href="{{ path('inscription', { type: 'tuteur' }) }}">Tuteur</a>
                </nav>
            </div>
</header>